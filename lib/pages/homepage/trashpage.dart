import 'package:flutter/material.dart';

class TrashPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Çöp Kutusu'),
      ),
      body: Center(
        child: Text('Çöp Kutusu Sayfası'),
      ),
    );
  }
}
