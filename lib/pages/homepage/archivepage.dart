import 'package:flutter/material.dart';

class ArchivePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Arşiv'),
      ),
      body: Center(
        child: Text('Arşiv Sayfası'),
      ),
    );
  }
}
